<!DOCTYPE html>
<html lang="<%= locals?.tenant?.preferences?.lang || 'en-gb' %>">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>ezBudget - <%= title %></title>
    <style>
      :not(:defined) {
        display: none;
      }

      @font-face {
        font-family: "Material Symbols Rounded";
        font-style: normal;
        font-weight: 100 700;
        font-display: swap;
        src: url("/assets/sykg-zNym6YjUruM-QrEh7-nyTnjDwKNJ_190Fjzag.woff2")
          format("woff2");
      }

      .material-symbols-rounded {
        font-family: "Material Symbols Rounded";
        font-weight: normal;
        font-style: normal;
        font-size: 24px;
        line-height: 1;
        letter-spacing: normal;
        text-transform: none;
        display: inline-block;
        white-space: nowrap;
        word-wrap: normal;
        direction: ltr;
        -webkit-font-feature-settings: "liga";
        -webkit-font-smoothing: antialiased;
      }
    </style>
    <% if(locals?.tenant?.preferences?.theme === 'light') { %>
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/@shoelace-style/shoelace@2.12.0/cdn/themes/light.css"
    />
    <% } else if(locals?.tenant?.preferences?.theme === 'dark') { %>
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/@shoelace-style/shoelace@2.12.0/cdn/themes/dark.css"
      onload="document.documentElement.classList.add('sl-theme-dark');"
    />
    <% } else { %>
    <link
      rel="stylesheet"
      media="(prefers-color-scheme:light)"
      href="https://cdn.jsdelivr.net/npm/@shoelace-style/shoelace@2.12.0/cdn/themes/light.css"
    />
    <link
      media="(prefers-color-scheme:dark)"
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/@shoelace-style/shoelace@2.12.0/cdn/themes/dark.css"
      onload="document.documentElement.classList.add('sl-theme-dark');"
    />

    <% } %>
    <link rel="stylesheet" href="/css/main.css" />
    <script
      type="module"
      src="https://cdn.jsdelivr.net/npm/@shoelace-style/shoelace@2.12.0/cdn/shoelace.js"
    ></script>
    <script type="module" src="/scripts/components/index.js"></script>
    <script type="module" src="/scripts/main.js"></script>
    <%- defineContent('styles') %> <%- defineContent('scripts') %>
  </head>

  <body class="app">
    <%- include('partials/header') %>
    <section class="main-content">
      <%- include('partials/sidebar') %> <%- body %>
    </section>
    <%- include('partials/footer') %>

    <sl-alert id="toast" variant="warning" duration="3500" closable>
      <div slot="icon" name="icon"><i class="mdi large">warning</i></div>
      <h3 style="font-weight: 900">Title</h3>
      <span>Message</span>
    </sl-alert>

    <sl-dialog id="dialog" label="Dialog">
      <div slot="content">
        <p>Content</p>
      </div>
    </sl-dialog>

    <%- defineContent('templates') %>
  </body>
</html>
